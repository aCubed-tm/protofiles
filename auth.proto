syntax = "proto3";

package proto;

service LoginService {
    rpc Login (LoginRequest) returns (LoginReply) {
    }
}

message LoginRequest {
    string email = 1;
    string password = 2;
}

message LoginReply {
    bool success = 1;
}
